<flash id="flash-messages">
  <% flash.each do |key, value| %>
    <% key = (key == 'alert') ? 'danger' : key %>
    <% key = (key == 'notice') ? 'info' : key %>
    <div class="alert alert-<%= key %>"><%= value %></div>
  <% end %>
</flash>
